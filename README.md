# dockerDjangoPostgresql

Startup Template

// Create File

docker-compose run django django-admin startproject core .

//Run Docker

docker-compose up

// Check Status

docker ps

// Change directory to docker file location

docker exec -it django bash

